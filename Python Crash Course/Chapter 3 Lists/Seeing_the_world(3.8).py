# Store the locations in a list. Make sure the list is not in alphabetical order
# Print your list in its iriginal order
# Use sorted() to print your list in alphabetical order
# Show that your list is still in its original order by printing it
# Use sorted() to print your list in reverse alphaetical order without changing the order of the original list
# Show that your list is still in its original order by printing it again
# Use reverse() to change the order of your list. Print th elist to show tha tirs order has changed
# Use reverse() to change the order of your list again. Print the list to show it's back that its order has been changed
# Use sort() to change your list so its stired in alphabetical order.
# Use sort() to change your list so its stored in in reverse alphabetical order

places = ['London', 'Paris', 'New Zealand', 'Tokyo', 'Vancouver', 'Barcelona', 'California']

print(places)
print(sorted(places))
print(places)

places.sorted(reverse=True)
print(places)
